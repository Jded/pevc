import { MaterialTypePipe } from './material-type.pipe';

describe('MaterialTypePipe', () => {
  it('create an instance', () => {
    const pipe = new MaterialTypePipe();
    expect(pipe).toBeTruthy();
  });
});
